! read data from the input

      program read_code

        INTEGER :: a, b, c

        print *, "Enter the data!"
        read *, a, b, c

        print *, "a = ", a, "b = ", b, "c = ", c

      end program read_code
