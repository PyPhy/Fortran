      program code

        implicit none
        INTEGER :: a, b
        REAL :: c, d

        a = 3
        b = 4

        c = real(a)
        d = real(b)

        ! similarly int()

        print *, "a = ", a, "b = ", b
        print *, "c = ", c, "d = ", d

      end program code
