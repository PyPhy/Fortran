      program strings

        implicit none
        character :: a ! a can store only one character
        character (len = 5) :: b ! b is 5 character long string

        ! other ways to define strings
        ! character(5) = b
        ! character*5 = b
        ! character = b*5

        a = 'X'
        b = 'white'

      end program strings
